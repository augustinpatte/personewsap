/**
 * The one official PersoNewsAP contact address.
 *
 * Support, privacy and general inquiries all go here: there is no second
 * mailbox, so there is no second constant. Every page that shows an address
 * reads this one, which is what keeps /support, /privacy, /delete-account and
 * the site footer from ever disagreeing.
 */
export const CONTACT_EMAIL = "contact@personewsap.com";

export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}`;
